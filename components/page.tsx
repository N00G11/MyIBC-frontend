import { Suspense } from 'react'// ...existing imports...export const dynamic = 'force-dynamic'// Créer un composant séparé pour la logique du badgefunction BadgeContent() {  // ...existing code with useSearchParams()...}// Composant de fallback pendant le chargementfunction BadgeLoading() {  return (    <div className="flex items-center justify-center min-h-screen">      <div className="text-center">        <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-blue-600 mx-auto mb-4"></div>        <p>Chargement du badge...</p>      </div>    </div>  )}export default function BadgePage() {  return (    <Suspense fallback={<BadgeLoading />}>      <BadgeContent />    </Suspense>  )
}
